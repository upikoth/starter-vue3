import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'upikoth.github.io',
  appName: 'starter-vue3',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
